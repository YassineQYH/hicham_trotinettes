<nav class="menu-nav">
    <!-- logo -->
    <a href="/" class="logo">
        <img src="{{ asset('assets/img/logo-pergolazur-nav.png') }}" alt="Logo Pergolazur" />
    </a>

    {% for message in app.flashes('notice') %}
        <div class="alert alert-info" style="color: orange; font-weight: bold;">
            {{ message }}
        </div>
    {% endfor %}

    <!-- menu-btn -->
    <input class="menu-btn" type="checkbox" id="menu-btn"/>
    <label class="menu-icon" for="menu-btn">
        <span class="nav-icon"></span>
    </label>

    <!-- menu -->
    <ul class="menu">
        <li><a href="/">Accueil</a></li>

        {% for trotinette in trotinettes_menu %}
            <li><a href="/trotinette/{{ trotinette.slug }}">{{ trotinette.name }}</a></li>
        {% endfor %}

        <li><a href="/#contact">Contact</a></li>
        <li><a href="/login">Connexion</a></li>
    </ul>
</nav>
