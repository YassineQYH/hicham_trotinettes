<nav class="menu-nav">
    <!-- logo -->
    <a href="{{ path('app_home') }}" class="logo">
        <img src="{{ asset('/img/logo-mhvolt.png') }}" alt="Logo Hich" />
    </a>

    <!-- messages flash -->
    {# {% for message in app.flashes('notice') %}
        <div class="alert alert-info" style="color: orange; font-weight: bold;">
            {{ message }}
        </div>
    {% endfor %} #}

    <!-- menu mobile toggle -->
    <input class="menu-btn" type="checkbox" id="menu-btn"/>
    <label class="menu-icon" for="menu-btn">
        <span class="nav-icon"></span>
    </label>

    {% set cart = cart() %} {# Panier global #}

    <!-- menu principal -->
    <ul class="menu">
        <li><a href="{{ path('app_home') }}">Accueil</a></li>

        <li class="menu-deroulant">
            <a href="javascript:void(0)">Nos trottinettes <span><i class="fas fa-chevron-down"></i></span></a>
            <ul class="sous-menu-trottinettes">
                {% for trottinette in trottinettes_menu %}
                    <li class="trottinette-pair">
                        <a href="{{ path('trottinette_show', {'slug': trottinette.slug}) }}">
                            {{ trottinette.nameShort }}
                        </a>
                        <span class="separator">|</span>
                        <a class="accessoire-item" href="{{ path('trottinette_accessoires', {'slug': trottinette.slug}) }}">
                            Accessoires
                        </a>
                    </li>
                {% endfor %}

                <li style="color:white;">
                    {% set total = cart|reduce((carry, item) => carry + (item.product.price * item.quantity), 0) %}
                    <small><strong>{{ (total / 100)|number_format(2, ',','.') }} €</strong></small>
                </li>
            </ul>
        </li>

        <li><a href="/#contact">Nous contacter</a></li>

        <!-- 🛒 Panier -->
        <li class="menu-panier">
            {{ include('partials/cart.html.twig', {
                cart: cart,
                cartObject: cart
            }) }}
        </li>

        {% if app.user %}
        <button class="btnLogin-popup">Mon Compte</button>
        {% else %}
        <button class="btnLogin-popup">Connexion</button>
        {% endif %}
    </ul>
</nav>
