<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Étiquette Interne {{ order.reference }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            margin: 0;
            padding: 5px;
        }
        .etiquette {
            width: 105mm;
            height: auto;
            padding: 5mm;
            border: 1px solid #000;
            box-sizing: border-box;
        }
        h1, h2 {
            margin: 0;
            padding: 0;
        }
        h1 {
            font-size: 14px;
            margin-bottom: 5px;
        }
        h2 {
            font-size: 12px;
            margin-top: 5px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 5px;
            font-size: 11px;
        }
        th, td {
            border: 1px solid #000;
            padding: 3px;
            text-align: left;
        }
        .client-qr {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-top: 5px;
        }
        .client-info {
            width: 65%;
        }
        .qr {
            width: 30%;
            text-align: center;
        }
        .summary {
            margin-top: 10px;
            font-size: 11px;
        }
        .summary table {
            width: 100%;
            border-collapse: collapse;
        }
        .summary td {
            border: none;
            padding: 2px;
        }
        .summary .label {
            font-weight: bold;
        }
        .footer {
            font-size: 10px;
            margin-top: 5px;
            color: gray;
        }
    </style>
</head>
<body>
<div class="etiquette">
    <h1>Étiquette Interne - Commande {{ order.reference }}</h1>

    <div class="client-qr">
        <div class="client-info">
            <h2>Client</h2>
            <p>
                {{ order.user.firstName }} {{ order.user.lastName }}<br>
                {% if order.user.phoneNumber is defined %}{{ order.user.phoneNumber }}<br>{% endif %}
                {{ order.delivery|raw }}
            </p>
        </div>
        <div class="qr">
            {% if order.trackingNumber %}
                <img src="https://api.qrserver.com/v1/create-qr-code/?data={{ order.trackingNumber }}&size=100x100" alt="QR Code">
            {% endif %}
        </div>
    </div>

    <h2>Produits</h2>
    <table>
        <thead>
            <tr>
                <th>Produit</th>
                <th>Qté</th>
                <th>Poids</th>
                <th>Prix unitaire</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% for detail in order.orderDetails %}
                <tr>
                    <td>{{ detail.product }}</td>
                    <td>{{ detail.quantity }}</td>
                    <td>{{ detail.weight }} kg</td>
                    <td>{{ detail.price|number_format(2, ',', ' ') }} €</td>
                    <td>{{ (detail.price * detail.quantity)|number_format(2, ',', ' ') }} €</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="summary">
        <h3>Informations commande</h3>
        <table>
            {% set totalHT = 0 %}
            {% for item in order.orderDetails %}
                {% set totalHT = totalHT + (item.price * item.quantity) %}
            {% endfor %}
            {% set tvaRate = 0.20 %}
            {% set tvaAmount = totalHT * tvaRate %}
            {% set totalPanier = totalHT + tvaAmount %}
            {% set totalTTC = totalPanier + order.carrierPrice %}

            <tr>
                <td><strong>Passée le :</strong></td>
                <td>{{ order.createdAt|date('d/m/Y H:i') }}</td>
            </tr>
            <tr>
                <td><strong>Frais de livraison :</strong></td>
                <td>{{ order.carrierPrice|number_format(2, ',', ' ') }} €</td>
            </tr>
            <tr>
                <td><strong>Total panier :</strong></td>
                <td>{{ totalPanier|number_format(2, ',', ' ') }} €</td>
            </tr>
            <tr>
                <td><strong>Total commande :</strong></td>
                <td>{{ totalTTC|number_format(2, ',', ' ') }} €</td>
            </tr>
            <tr>
                <td><strong>Transporteur :</strong></td>
                <td>{{ order.carrier ?? order.carrier }}</td>
            </tr>
        </table>
    </div>

    <div class="footer">
        Étiquette interne à apposer sur le colis — usage entrepôt uniquement.
    </div>
</div>
</body>
</html>
